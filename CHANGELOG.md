# Changelog

## 1.0.0 — 2026-09

Первый публичный выпуск: `install-node.sh` (нода/мост одной командой, защита существующей
установки, `--force` с бэкапами, geodata с проверкой размера, ufw по `--panel-ip`),
`tune-net.sh` (BBR, `--undo`), `tune-resilience.sh` (conntrack по памяти, очередь приёма,
watchdog, panic-ребут, earlyoom, своп; `--dry-run`, `--undo`), `geodata-update.sh`
(проверка + откат, `--install-timer`), `probe-speed.sh`, `probe-steal.sh`, `health.sh`,
`fleet.sh`; документы START-HERE, SAFETY, tuning, faq.
